# Briefing verbatim — Research: Auditoria Backend FUP S4 vs cap-clean-architecture

quero sim, use a sua skill de /deep-research pra isso

Contexto: Auditar o backend `suzano-fup-panel-backend-s4` (3 serviços CAP + db + app-router) arquivo a arquivo contra o standard `cap-clean-architecture` em `docs/standards/cap-clean-architecture/`. 

Objetivo: Produzir insumo cirúrgico para criar épicos e subtasks no Jira (board RFIA, ID 2011). Cada violação deve ser rastreável a arquivo:linha e regra do standard quebrada.

O código que funciona já está em produção — a refatoração precisa preservar comportamento.
